ext {
    set("extName", "Scan-Manga")
    set("pkgNameSuffix", "fr.scanmanga")
    set("extClass", ".ScanManga")
    set("extVersionCode", 1)
    set("libVersion", "1.4")
}

apply(from = "$rootDir/common.gradle")
